import React from 'react'

type Props = {
    title: string,
    hero: string,
    overview: string,
    popularity: number,
    poster: string,
    releaseDate: string,
    voteAverage: number,
    voteCount: number,
}

const MediaCard = (props:Props) => {
    return <div>Media Card</div>
}

export default MediaCard