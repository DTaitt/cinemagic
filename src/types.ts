export type Show = {
    id: string,
    name: string,
    hero: string,
    overview: string,
    popularity: number,
    poster: string,
    releaseDate: string,
    voteAverage: number,
    voteCount: number,
}